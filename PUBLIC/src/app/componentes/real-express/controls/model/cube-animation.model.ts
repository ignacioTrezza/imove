export class CubeAnimation {
    rotationSpeed: number;
  
    constructor() {
      this.rotationSpeed = 1.0; // Default rotation speed
    }
  
    updateAnimation() {
      // Emit event or call service to update the animation properties
    }
  }